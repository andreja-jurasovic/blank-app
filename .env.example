# HAOD Digital Assistant (Miran) - Environment Variables
# Copy this file to .env and fill in your values

# Google Gemini API Key (required for LLM features)
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Model configuration
# Options: gemini-1.5-flash (default), gemini-1.5-pro, gemini-1.0-pro
GEMINI_MODEL=gemini-1.5-flash

# Fallback model if primary hits rate limits
GEMINI_FALLBACK_MODEL=gemini-1.5-flash

# Classification mode
# "llm" = Use Gemini for classification (recommended for demo, more accurate)
# "rules" = Use pattern matching only (no API calls, works offline)
CLASSIFICATION_MODE=llm
